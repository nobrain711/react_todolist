import styled from "styled-components";

export const StyledTitle = styled.div`
  font-size: 5.5rem;
  text-shadow: 1rem 1rem 0.5rem gray;
  margin: 1rem 0;
`;

export const StyledHeader = styled.div`
  display: grid;
  place-items: center;
`;
